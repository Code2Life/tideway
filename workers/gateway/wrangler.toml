name = "tideway"
main = "src/index.ts"
compatibility_date = "2026-02-08"
workers_dev = true
account_id = "0a406b93a2dfc733d6cd3ec64ac6ad42"

# Routes will be configured via dashboard after deployment
# routes = [
#   { pattern = "sse.tensor-fusion.ai/*", zone_name = "tensor-fusion.ai" }
# ]

[[durable_objects.bindings]]
name = "GATEWAY_ROOM"
class_name = "GatewayRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["GatewayRoom"]

# [vars]
# SSE_PUBLISHER_API_KEYS = "dev-key" (for test env only)
# wrangler secret put SSE_PUBLISHER_API_KEYS

[observability]
enabled = true
